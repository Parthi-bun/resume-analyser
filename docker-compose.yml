version: "3.9"

services:
  resume-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-resume-analyzer
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
      - "8501:8501"
